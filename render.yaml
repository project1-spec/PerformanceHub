services:
  - type: web
    name: performancehub
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py --port=$PORT
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.0
